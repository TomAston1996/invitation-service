service: invitation-service

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x
  region: eu-west-2

functions:
  test:
    handler: index.test
    events:
      - http:
        path: /test
        method: get
